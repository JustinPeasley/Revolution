<?xml version="1.0" encoding="utf-8"?>
<!-- XML for the newGame ImageButton -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:background="@color/light_green">

    <!-- used to format text to right of numberPicker-->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@android:color/transparent">
        <!-- header for dialog box -->
        <TextView android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:textSize="24sp"
            android:textStyle="bold"
            android:background="@android:color/transparent"
            android:text="@string/restartButtonDescription"
            />

        <!-- prompt user on numberPicker usage -->
        <TextView
            android:layout_gravity="right"
            android:layout_marginTop="@dimen/linear_layout_margin_vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/enter_depth"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingRight="16dp"/>

    </LinearLayout>
    <!--numberPicker places to the right of text -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

            <!-- NumberPicker for selecting solDepth -->
            <NumberPicker
                android:id="@+id/solDepthPicker"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                />
    </LinearLayout>

</LinearLayout>


